gui:
  shrinkSidePanelsToContent: true
  expandFocusedSidePanel: true
  expandedSidePanelWeight: 2
  sidePanelWidth: 0.32
  nerdFontsVersion: 3
  filterMode: fuzzy
  theme:
    selectedLineBgColor:
      - bold
      - '#404040'
  commitHashLength: 5

  sidePanels:
    - [status]
    - [files, worktrees, submodules]
    - [branches, remotes, tags]
    - [commits, reflog]

  spinner:
    frames:
      # https://andreichiril.neocities.org/braille
      - '⣏⣀ '
      - '⣏⡁ '
      - '⣏⠉ '
      - '⡏⠙ '
      - '⠏⠹ '
      - '⠋⢹ '
      - '⠉⣹ '
      - '⢈⣹ '
      - '⣀⣹ '
      - '⣄⣸ '
      - '⣆⣰ '
      - '⣇⣠ '

git:
  diffRenderers:
    - command: delta --config $HOME/.config/delta/.gitconfig --paging=never --features='unified-view'
    - command: delta --config $HOME/.config/delta/.gitconfig --paging=never --features='split-view'
    # - externalDiffCommand: difft
    # - useExternalDiffGitConfig: true

notARepository: skip
promptToReturnFromSubprocess: false










